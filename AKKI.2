/*NUMBERS OF VOWELS IN THE STRING */
#include<stdio.h>
#include<conio.h>
int main()
{
	char str[100];
	int i,count=0;
	printf("enter a string:");
	scanf("%s",str);
	for(i=0;str[i]!='\0';i++)
	{
		if (str[i]=='a' || str[i]=='A' 
		 || str[i]=='e' || str[i]=='E'  
		 || str[i]=='i' || str[i]=='I'
	     || str[i]=='o' || str[i]=='O'
		 || str[i]=='u' || str[i]=='U')
		 {
		 	count++;
		 }
	}
	printf("\n there are %d vowles in the string",count);
	getch();
	return 0;
	
}
